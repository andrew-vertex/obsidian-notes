# 知识库 Agent 规范

本仓库是个人 Obsidian 知识库。任何 AI Agent 的核心目标都是：**协助用户进行笔记的精细整理、分类归档与索引同步**，保持知识库结构清爽直观。

---

## 目录结构约定

物理知识分类采用中文一级目录，不搞复杂的深层嵌套：

- `1. 技术研发/` — AI应用开发、后端与架构、DevOps与运维、算法与编程。
- `2. 实用工具/` — AI工具手册、Mac效率工具指南。
- `3. 工作与业务/` — 保险核赔业务设计、工单与排障、个人履历与项目经历。
- `4. 思考与认知/` — 思维模型、语言学习、读书与感悟。
- `5. 项目与作品/` — 个人项目、博客与文章。
- `6. 收集箱/` — 临时文件、草稿、网页剪藏入口。
- `wiki/` — 极简 AI 控制层（固定只有 `overview.md`, `hot.md`, `goals.md`, `index.md`, `log.md` 5 个文件）。

---

## Agent 核心行为规则

### 1. 专注整篇整理，严禁自动建碎卡片
- **严禁**建立 `wiki/concepts/` 或 `wiki/entities/` 碎片卡片。
- 当用户要求整理资料或记录新笔记时，AI 必须将其重构成**一整篇结构完整、带 Headline、层次分明、富 Markdown 语法的单篇笔记**。

### 2. 物理归位原则
- 所有的正式笔记必须直接存储在对应的 `1. 技术研发/` 到 `5. 项目与作品/` 中文子目录下。
- 严禁将正式笔记乱丢在 `wiki/` 或 `6. 收集箱/` 中。

### 3. 控制层同步规则
每次新增或更新笔记后：
- 在 `wiki/log.md` 顶部记录一行变更记录。
- 更新 `wiki/index.md` 中的链接表。
- 如涉及重大焦点变化，更新 `wiki/hot.md`。

---

## 笔记格式规范

- 所有正式笔记包含标准 Frontmatter（`title`, `created`, `updated`, `tags`）。
- 内容优先使用“渐进式摘要”：Key Takeaways 前置、表格对比、加粗关键结论。
