# 核赔
## 公共池 `/case/publicPoolCase`

### `tc_divisional_case` 字段使用表

| 字段                    | 使用方式     |
| --------------------- | -------- |
| `state`               | 筛选、返回    |
| `subState`            | 筛选、返回    |
| `checkRule`           | 筛选       |
| `realIndemnityAmount` | 筛选       |
| `divisionalCaseId`    | 筛选、返回、排序 |
| `divisionalCaseNo`    | 查询结果、返回  |
| `icDivisionalCaseNo`  | 返回       |
| `priority`            | 返回       |
| `generalCaseId`       | 筛选       |

## 个人池 `/case/personal`

### `tc_divisional_case` 字段使用表

| 字段                    | 使用方式  |
| --------------------- | ----- |
| `state`               | 筛选、返回 |
| `subState`            | 筛选、返回 |
| `checkRule`           | 筛选    |
| `oldState`            | 计算返回值 |
| `divisionalCaseId`    | 返回、排序 |
| `divisionalCaseNo`    | 返回、分组 |
| `realUnitName`        | 返回    |
| `totalAmount`         | 返回    |
| `deductionMemo`       | 返回    |
| `calcIndemnityAmount` | 返回    |
| `realIndemnityAmount` | 返回    |
| `priority`            | 返回    |

## 申请 `/case/apply` 
### `tc_divisional_case` 字段使用表

| 字段                 | 使用方式  |
| ------------------ | ----- |
| `divisionalCaseNo` | 筛选，使用 |
| `state`            | 筛选，返回 |
| `checkRule`        | 筛选    |
| `divisionalCaseId` | 筛选，使用 |

## 指派`/case/assign`

### `tc_divisional_case` 字段使用表

| 字段                 | 使用方式  |
| ------------------ | ----- |
| `divisionalCaseNo` | 筛选，使用 |
| `state`            | 筛选，返回 |
| `divisionalCaseId` | 筛选，使用 |

# 中台

## 公共池 `/case/public`

### `tc_divisional_case` 字段使用表

|字段|使用方式|
|---|---|
|`divisionalCaseId`|查询结果、返回、排序、分组、关联|
|`generalCaseId`|关联|
|`state`|筛选、返回|
|`divisionalCaseNo`|筛选、返回|
|`backState`|筛选、返回|
|`haveWrongState`|筛选、计算返回值|
|`realIndemnityAmount`|筛选、返回|
|`refused`|筛选、返回|
|`adjustmentCheckTime`|筛选、返回|
|`mcardId`|关联筛选|
|`icDivisionalCaseNo`|筛选、返回|
|`totalAmount`|返回|
|`calcIndemnityAmount`|返回|
|`deductionMemo`|返回|
|`doubtReason`|返回|
|`errorMsg`|返回|
|`priority`|返回|
|`proType`|返回|
|`outputRemark`|返回|
|`remitState`|返回|
|`oldState`|返回|
|`spiltFlag`|返回|
|`realUnitId`|返回|
|`realUnitName`|返回|
|`createTime`|计算返回值|

## 个人池 `/case/personal`

### `tc_divisional_case` 字段使用表

|字段|使用方式|
|---|---|
|`divisionalCaseId`|查询结果、返回、排序、分组、关联|
|`generalCaseId`|关联|
|`state`|筛选、返回|
|`divisionalCaseNo`|筛选、返回|
|`backState`|筛选、返回|
|`haveWrongState`|筛选、计算返回值|
|`realIndemnityAmount`|筛选、返回|
|`refused`|筛选、返回|
|`adjustmentCheckTime`|筛选、返回|
|`mcardId`|关联筛选|
|`icDivisionalCaseNo`|筛选、返回|
|`totalAmount`|返回|
|`calcIndemnityAmount`|返回|
|`deductionMemo`|返回|
|`doubtReason`|返回|
|`errorMsg`|返回|
|`priority`|返回|
|`proType`|返回|
|`outputRemark`|返回|
|`remitState`|返回|
|`oldState`|返回|
|`spiltFlag`|返回|
|`realUnitId`|返回|
|`realUnitName`|返回|
|`createTime`|计算返回值|


## 申请 `/case/apply`

### `tc_divisional_case` 字段使用表

|字段|使用方式|
|---|---|
|`divisionalCaseId`|筛选、使用、返回|
|`state`|筛选、返回|
|`divisionalCaseNo`|使用、返回|

## 指派 `/case/assign`

### `tc_divisional_case` 字段使用表

|字段|使用方式|
|---|---|
|`divisionalCaseId`|筛选、使用、返回|
|`divisionalCaseNo`|使用、返回|
